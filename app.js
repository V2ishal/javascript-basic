//q3 vovels count using normal for of loopes
let student="nayan";
let count=0;

for(let i=0; i<student.length; i++){
    let ch=student[i];

    if(ch==="a"||ch==="e"||ch==="i"||ch==="o"||ch==="u"){
        count++
    }
}
console.log(count);
